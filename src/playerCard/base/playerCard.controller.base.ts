/*
------------------------------------------------------------------------------ 
This code was generated by Amplication. 
 
Changes to this file will be lost if the code is regenerated. 

There are other ways to to customize your code, see this doc to learn more
https://docs.amplication.com/docs/how-to/custom-code

------------------------------------------------------------------------------
  */
import * as common from "@nestjs/common";
import * as swagger from "@nestjs/swagger";
import * as nestAccessControl from "nest-access-control";
import * as defaultAuthGuard from "../../auth/defaultAuth.guard";
import { isRecordNotFoundError } from "../../prisma.util";
import * as errors from "../../errors";
import { Request } from "express";
import { plainToClass } from "class-transformer";
import { ApiNestedQuery } from "../../decorators/api-nested-query.decorator";
import { PlayerCardService } from "../playerCard.service";
import { AclValidateRequestInterceptor } from "../../interceptors/aclValidateRequest.interceptor";
import { AclFilterResponseInterceptor } from "../../interceptors/aclFilterResponse.interceptor";
import { PlayerCardCreateInput } from "./PlayerCardCreateInput";
import { PlayerCardWhereInput } from "./PlayerCardWhereInput";
import { PlayerCardWhereUniqueInput } from "./PlayerCardWhereUniqueInput";
import { PlayerCardFindManyArgs } from "./PlayerCardFindManyArgs";
import { PlayerCardUpdateInput } from "./PlayerCardUpdateInput";
import { PlayerCard } from "./PlayerCard";
@swagger.ApiBearerAuth()
@common.UseGuards(defaultAuthGuard.DefaultAuthGuard, nestAccessControl.ACGuard)
export class PlayerCardControllerBase {
  constructor(
    protected readonly service: PlayerCardService,
    protected readonly rolesBuilder: nestAccessControl.RolesBuilder
  ) {}

  @common.UseInterceptors(AclValidateRequestInterceptor)
  @nestAccessControl.UseRoles({
    resource: "PlayerCard",
    action: "create",
    possession: "any",
  })
  @common.Post()
  @swagger.ApiCreatedResponse({ type: PlayerCard })
  @swagger.ApiForbiddenResponse({ type: errors.ForbiddenException })
  async create(
    @common.Body() data: PlayerCardCreateInput
  ): Promise<PlayerCard> {
    return await this.service.create({
      data: data,
      select: {
        cardId: true,
        createdAt: true,
        dungeonUsesRemaining: true,
        id: true,
        name: true,
        updatedAt: true,
      },
    });
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @nestAccessControl.UseRoles({
    resource: "PlayerCard",
    action: "read",
    possession: "any",
  })
  @common.Get()
  @swagger.ApiOkResponse({ type: [PlayerCard] })
  @swagger.ApiForbiddenResponse()
  @ApiNestedQuery(PlayerCardFindManyArgs)
  async findMany(@common.Req() request: Request): Promise<PlayerCard[]> {
    const args = plainToClass(PlayerCardFindManyArgs, request.query);
    return this.service.findMany({
      ...args,
      select: {
        cardId: true,
        createdAt: true,
        dungeonUsesRemaining: true,
        id: true,
        name: true,
        updatedAt: true,
      },
    });
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @nestAccessControl.UseRoles({
    resource: "PlayerCard",
    action: "read",
    possession: "own",
  })
  @common.Get("/:id")
  @swagger.ApiOkResponse({ type: PlayerCard })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  @swagger.ApiForbiddenResponse({ type: errors.ForbiddenException })
  async findOne(
    @common.Param() params: PlayerCardWhereUniqueInput
  ): Promise<PlayerCard | null> {
    const result = await this.service.findOne({
      where: params,
      select: {
        cardId: true,
        createdAt: true,
        dungeonUsesRemaining: true,
        id: true,
        name: true,
        updatedAt: true,
      },
    });
    if (result === null) {
      throw new errors.NotFoundException(
        `No resource was found for ${JSON.stringify(params)}`
      );
    }
    return result;
  }

  @common.UseInterceptors(AclValidateRequestInterceptor)
  @nestAccessControl.UseRoles({
    resource: "PlayerCard",
    action: "update",
    possession: "any",
  })
  @common.Patch("/:id")
  @swagger.ApiOkResponse({ type: PlayerCard })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  @swagger.ApiForbiddenResponse({ type: errors.ForbiddenException })
  async update(
    @common.Param() params: PlayerCardWhereUniqueInput,
    @common.Body() data: PlayerCardUpdateInput
  ): Promise<PlayerCard | null> {
    try {
      return await this.service.update({
        where: params,
        data: data,
        select: {
          cardId: true,
          createdAt: true,
          dungeonUsesRemaining: true,
          id: true,
          name: true,
          updatedAt: true,
        },
      });
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new errors.NotFoundException(
          `No resource was found for ${JSON.stringify(params)}`
        );
      }
      throw error;
    }
  }

  @nestAccessControl.UseRoles({
    resource: "PlayerCard",
    action: "delete",
    possession: "any",
  })
  @common.Delete("/:id")
  @swagger.ApiOkResponse({ type: PlayerCard })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  @swagger.ApiForbiddenResponse({ type: errors.ForbiddenException })
  async delete(
    @common.Param() params: PlayerCardWhereUniqueInput
  ): Promise<PlayerCard | null> {
    try {
      return await this.service.delete({
        where: params,
        select: {
          cardId: true,
          createdAt: true,
          dungeonUsesRemaining: true,
          id: true,
          name: true,
          updatedAt: true,
        },
      });
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new errors.NotFoundException(
          `No resource was found for ${JSON.stringify(params)}`
        );
      }
      throw error;
    }
  }
}
